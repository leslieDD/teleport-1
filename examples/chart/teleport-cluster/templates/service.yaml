apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
spec:
  type: LoadBalancer
  ports:
    - name: https
      port: 443
      targetPort: 3080
      protocol: TCP
    - name: k8s
      port: 3026
      targetPort: 3026
      protocol: TCP      
  selector:
    app: {{ .Release.Name }}
